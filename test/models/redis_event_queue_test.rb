require 'test_helper'

module PushyResources

  class RedisEventQueueTest < ActiveSupport::TestCase

    test "uses synchrony driver when requested from inside the EM reactor thread" do
    end

    test "uses ruby driver when requested from outside the EM reactor thread" do
    end

  end
end